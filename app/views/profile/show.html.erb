<%= render partial: 'shared/navbar' %>

<div class="container">
  <div class="col s2"></div>
  <div class="col s8">
    <%= image_tag current_user.image_url, class: "responsive-img circle" %>
    <h2> <%= current_user.nickname %> </h2>
    <h5> <%= current_user.bio %> </h5>
    <h5> <%= current_user.website %> </h5>
  </div>
  <div class="col s2"></div>
</div>
